hydra:
  run:
    dir: runs/${task.task}/${name}

name: v9-dev

# Dual assignment training and suppression-free inference for YOLOv9 detection.
model:
  nms_free: false

defaults:
  - _self_
  - task: inference
  - dataset: coco
  - model: v9-c
  - qat: default
  - general
