accelerator: auto
precision: 16-mixed
device: auto
cpu_num: 16

image_size: [640, 640]

out_path: runs
exist_ok: True

lucky_number: 10 # Seed for model initialization, data shuffle, and augmentation
use_wandb: True
use_tensorboard: False

weight: True # Path to weight or True for auto, False for no pretrained weight
