task: validation

evaluator: coco-pose
annotation_path: null

data:
  batch_size: 32
  image_size: ${image_size}
  cpu_num: ${cpu_num}
  shuffle: false
  pin_memory: true
  data_augment: {}
  dynamic_shape: false
nms:
  min_confidence: 0.0001
  min_iou: 0.7
  max_bbox: 1000
